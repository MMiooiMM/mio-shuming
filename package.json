{
  "name": "mio-shuming",
  "private": true,
  "version": "0.1.0",
  "description": "數名其妙 — 康熙筆畫三才五格、立春換算生肖字根、五行分佈的姓名學分析",
  "license": "GPL-2.0-only",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "gen:data": "node tools/gen-kangxi-strokes.mjs && node tools/gen-solar-terms.mjs && node tools/gen-components.mjs && node tools/gen-locations.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "jsdom": "^25.0.1",
    "typescript": "~5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.5"
  }
}
