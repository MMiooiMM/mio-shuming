# 數名其妙 v4 — 可分享的產品外殼（Frozen Acceptance）

> 由 /mio-hot-sauce grill 收斂，2026-09-13 使用者確認凍結。
> 本檔為 v4 開發、驗證（mio-fire-alarm）、獨立 review（diff-reviewer）的凍結判準。
> v1 判準見 [SPEC.md](./SPEC.md)，v2 見 [SPEC-v2.md](./SPEC-v2.md)，v3 見 [SPEC-v3.md](./SPEC-v3.md)。

## 背景

v1–v3 的計算引擎與出處追溯已完成，缺的是「讓人看得懂、敢分享、傳得開」的產品外殼。
本輪定位為**可分享的個人作品**：親友之間（多半經 LINE 傳給家人長輩）用得順、看起來專業；
不經營搜尋流量。

## A. 分析卡片（分享）

1. 分析結果區提供「分享卡片」按鈕，以**前端 Canvas** 繪製 PNG，**不新增執行期依賴套件**。
2. 卡片內容：姓名、生日（年月日）、生肖、四柱干支；外加**分項標籤並列**
   （三才、五格各格、生肖喜忌字根、用神匹配）——每項各自一個判定，**不計數、不加總**。
3. 卡片**不印**時分、出生地。未填時辰時，四柱與八字相關標籤不出現。
4. 卡片底部固定印：站名＋網址＋「僅供參考」。
5. 支援 Web Share（含檔案）的裝置叫出系統分享選單；不支援時退回下載 PNG。
   兩條路徑都要有 E2E 或實機證據。
6. 立春當日未定生肖等不確定情況，卡片據實呈現兩種可能，不擅自選一個。
7. **網址不帶任何使用者輸入**——全站不寫入 query／hash。

## B. 結果摘要與名詞解釋

8. 分析結果頂端有摘要區，與卡片使用**同一組**分項標籤（同一個產生函式）。
9. 名詞（人格、天格、地格、外格、總格、三才、用神、十神、藏干、真太陽時等）可就地點開說明；
   滑鼠與鍵盤（Enter／Space）皆可展開收合，並具正確的 `aria-expanded`。
10. 解釋文字集中於 `src/data/` 資料檔，不寫死在 UI 程式裡。

## C. 收藏比較（取名模式）

11. 收藏資料加存預產期；**舊的 `mio-shuming:favorites:v1` 資料仍可讀取**。
    無預產期的舊收藏，生肖標籤顯示「未知」，不猜。
12. 可勾選 2–5 個收藏並排比較：各名字的三才、五格（僅列取名可改變、計吉凶的格）、
    生肖喜忌字根標籤。
13. 比較結果可輸出比較卡片（輸出方式同 #5）；卡片**只印生肖、不印預產期**；
    預產期在立春 ±3 週內則印兩個生肖。

## D. 聲明

14. 頁尾常駐免責與隱私聲明，內容含：所有計算在裝置上完成、不上傳；
    收藏只存在此瀏覽器；命理結果僅供參考。
15. 兩種模式的出生資料表單旁，各有一行「資料只在你的裝置計算，不上傳」。

## E. 分享預覽

16. `index.html` 補上 OG／Twitter card meta、favicon、`apple-touch-icon`；OG 圖為固定圖片。
    以 LINE 或 FB 的預覽除錯工具實際抓取，截圖當證據。

## F. 品質門檻

17. 新增 Playwright E2E，在寬度 **390px** 與 **1280px** 各跑一遍：兩模式主流程、分析卡片、
    收藏比較與比較卡片、名詞解釋的鍵盤操作。
18. CI 流程為 `npm test` → Playwright → build，全過才部署；任一失敗即不部署。
19. 首頁、分析結果、取名結果三種狀態的 Lighthouse 無障礙分數皆 **≥ 95**，附報告數字當證據。
20. 390px 寬時整頁不可橫向捲動（表格限於自身容器內捲動），
    以 Playwright 斷言 `scrollWidth <= clientWidth` 驗證。
21. 既有 vitest 全數通過；動到畫面外觀的部分須走 `/mio-mirror`。

## G. UI 收斂第一輪（2026-09-13 追加凍結）

> 來源：[外部 UI 評審](./docs/design-review/2026-09-13-external-ui-critique.md)的「只能改三件事」第 1 項，範圍由使用者裁決。
> 改動前的截圖：`docs/evidence/v4-walkthrough/`（HEAD `7c39bf2`）。

**按鈕層級**

22. 三級按鈕：
    - **主要**：實心 `--accent`。
    - **次要**：`--surface` 底、1.5px `--accent` 框、`--accent` 字。
    - **文字**：無底色，`--ink-soft` 字，hover 時加底線。
    - 小按鈕統一高 36px、左右 padding 14px、圓角 8px。
23. 按鈕分配：
    - **主要**：列出吉筆畫組合、開始分析、比較勾選的名字、分享卡片。
    - **次要**：帶入完整分析、收藏這個名字、輸出比較卡片。
    - **文字**：移除。
    - 用神覆寫切換鈕不再用綠色，選中狀態改用 `--accent-soft` 底。
    - 模式切換大卡不動。

    > **2026-09-13 修正（H 節 #35）**：原分配把「分享卡片」列主要、「輸出比較卡片」列次要，同為匯出圖卡卻分兩級（第二輪評審實證）。兩者統一為次要。
    > 同節「選中狀態改用 `--accent-soft` 底」也由 #33 取代：選中態一律實心 `--accent` 配白字，因為粉紅與凶／忌同色系。
24. radio 與 checkbox 使用 `accent-color: var(--accent)`。

**語意色歸位**

25. 新增中性灰 token：淺色模式底 `#EEEBE6`、字 `#6B645A`（對比 4.91:1）；深色模式另訂，對比須 ≥ 4.5:1。
    - 改用灰色：「中性」「不計」「生肖未知」、用神五行標籤。
    - 黃色（`--mid`）只留給「半吉」與「喜忌並見」。
26. 候選字 chip：中性字白底（`--surface`）加 1px `--line` 框、`--ink` 字；喜字維持綠、忌字維持紅；選中為實心 `--accent`。
27. 兩張分享圖卡的中性色改為同一個灰；版式與 chip 形狀不動。

**開發者用語**

28. 以下所有狀態的畫面文字（`document.body.innerText`）不得含 `SPEC`、`localStorage`、`curl`、`kTotalStrokes`、`kRSUnicode`、`$comment`、`tools/`、`src/`：
    - 兩模式的所有結果狀態
    - 名詞說明全部展開時
    - 收藏與比較視圖
    - 頁尾
    - 資料來源區塊

    「localStorage」改寫為「只存在這台裝置的瀏覽器」；Unicode／Unihan 等引用來源名稱保留。
29. 人工整理的資料檔直接改寫出處備註的措辭。原文引文（「」內）一字不改，只改描述查證方式的開發者用語。由腳本產生的資料檔，改生成腳本後重新產生，不手改。
30. 新增 Playwright 守衛：走完 #28 列的所有狀態後讀取 `innerText`，斷言不含上述字詞。須做變異測試：放回一個禁用字，測試要紅。

**品質門檻**

31. 既有 vitest 與 E2E 全數通過；Lighthouse 三態無障礙分數仍 ≥ 95；以 `tools/raw/mirror-shots.mjs` 拍 390／1280 前後對照截圖，並再跑一次 `/mio-mirror`。

**本輪不做**：結果優先與降噪、手機排版破損、網頁與圖卡 chip 形狀統一、圖卡版式。

## H. UI 收斂第二輪（2026-09-13 追加凍結）

> 來源：[第二輪外部評審](./docs/design-review/2026-09-13-external-ui-critique-round2.md)。
> 這一輪專修 G 節條文本身造成的問題：#23 匯出鈕分級、選中態借用粉紅、#28 禁用字列舉不全。範圍由使用者裁決。

**元件層級**

32. 模式分頁改為 segmented control。
    - 外框：底 `#ECE8E2`、padding 4px、圓角 12px。
    - 選中項：`--surface` 底、`--accent` 字、`box-shadow: 0 1px 2px rgba(0,0,0,.08)`。
    - 未選項：透明底、`--ink-soft` 字（對比 6.01:1）。
    - 深色模式另訂，對比 ≥ 4.5:1。保留 `aria-pressed`。
33. **選中態通則**：所有切換鈕的選中態一律實心 `--accent` 配白字（8.26:1），不得使用任何語意色淡底（`--accent-soft`、`--good-soft`、`--bad-soft`、`--mid-soft`）。先套用到用神覆寫開關。
34. 名詞說明框改用中性底 `#F3F1EC`（內文對比 15.02:1，深色模式另訂），保留左側 3px `--accent` 線。
35. 「分享卡片」與「輸出比較卡片」統一為**次要**（外框）。

**資料來源文字**

36. 畫面文字不得含內部文件路徑、內部欄位名（例如「資料檔的 conflicts」）或 Markdown 語法。
    - 資料檔一律存純文字，UI 不做 Markdown 渲染。
    - 上游檔名與欄位名（`Unihan_IRGSources.txt`、`kBigFive`、`gsc_pinyin.csv`、`ids.txt`、`CJKRadicals.txt` 等）改用人話名稱，例如「Unicode 漢字資料庫（Unihan）」。
    - 來源連結 `href` 不變，連結文字改為網域加 ↗。
    - 由腳本產生的資料檔依 #29，改生成腳本後重新產生。

**守衛**

37. `e2e/no-dev-jargon.spec.ts` 保留原列舉字詞，並在同樣 12 個狀態加入類別規則，各類別都要做一次變異測試：
    - 檔案路徑：`/[w.-]+/[w./-]+.(md|ts|mjs|json)/`
    - 檔名：`/[w-]+.(txt|csv|xml|zip|md|json|mjs|ts)/`
    - Markdown 粗體：`/**[^*
]+**/`
    - Unicode 欄位名：`/k[A-Z][A-Za-z]+/`
    - 內部欄位名：`conflicts`
38. **同類元件一致性測試**：E2E 斷言「分享卡片」與「輸出比較卡片」的計算樣式（`background-color`、`border-color`、`color`）完全相同。

**品質門檻**

39. vitest 與 E2E 全數通過；Lighthouse 三態無障礙 ≥ 95；390／1280 前後對照截圖；第三次 `/mio-mirror`。

**本輪不做**：資料來源收合與降噪、送出後結果優先、比較表裁切、chip 形狀統一、圖卡版式、「三才　三才」標籤重複。

## 沿用原則（v1–v3 全數繼承）

不合成單一總分；每條規則出處寫進資料檔 `source` 欄位並於「資料來源」呈現；
流派衝突不擅自調和；引擎純函式；罕字與範圍外據實回報不猜；GPLv2。

## Out of Scope

網址帶參數分享或重算、列印版面、SEO 靜態頁、流量統計、回饋管道、字義與讀音資料、
候選字排序改良、PWA 與離線、國際化。

## Grill 決策紀錄

| 決策點 | 裁決 | 未採用的選項 |
|---|---|---|
| 產品定位 | 可分享的個人作品 | 經營流量的產品；只補專業度底線 |
| v4 範圍 | 分享＋聲明＋手機/無障礙/E2E＋摘要/收藏比較 | （四項全選） |
| 分享方式 | 只做圖片卡片（使用者提出） | 網址帶參數重算（出生資料留在聊天與瀏覽紀錄）；圖片＋網址 |
| 摘要形式 | 分項標籤並列 | 計數統計（變相總分，牴觸 SPEC #6）；一句話文字摘要 |
| 卡片個資 | 姓名＋生日＋生肖＋四柱 | 只有姓名＋生日；全部含時分與出生地 |
| 收藏比較 | 並排分項標籤＋可輸出卡片 | 只在頁面上並排 |
| 卡片輸出 | Web Share＋下載 PNG | 只下載 PNG |
| 品質門檻 | Playwright 進 CI 擋部署＋Lighthouse 無障礙 ≥ 95 | Playwright 只在本機跑 |
| 列印版面 | 不做 | 完整報告列印 |
| 聲明位置 | 頁尾常駐＋表單旁一行 | 首次進站彈窗確認 |
| 名詞解釋 | 就地點開說明 | 獨立名詞區塊 |
| 卡片署名 | 站名＋網址＋僅供參考 | 只印僅供參考 |
| 比較卡個資 | 只印生肖、不印預產期 | 印預產期＋生肖 |
| UI 修正第一輪（09-13 追加） | 按鈕分級＋語意色＋拿掉開發者用語 | 結果優先＋降噪；手機排版破損；先收工 |
| 圖卡範圍（09-13 追加） | 只跟著改語意色 | 統一 chip 形狀；完全不動 |
| 出處備註（09-13 追加） | 改資料檔措辭 | UI 渲染時過濾 |
| UI 修正第二輪（09-13 追加） | 修掉 G 節條文造成的殘留 | 資料來源收合＋說明限高；結果優先＋比較表裁切；先收工 |
| 上游檔名（09-13 追加） | 改人話名稱，檔名只留在連結 | 保留檔名，只清內部路徑 |
| 匯出鈕層級（09-13 追加） | 兩者都次要 | 兩者都主要 |
